/**
 * Frontend interface for refresh token request
 * This is a plain interface without decorators to avoid issues in frontend builds
 */
export interface IRefreshTokenRequest {
  refreshToken: string;
}


